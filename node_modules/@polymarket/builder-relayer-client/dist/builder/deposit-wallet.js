"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildDepositWalletBatchRequest = buildDepositWalletBatchRequest;
exports.buildDepositWalletCreateRequest = buildDepositWalletCreateRequest;
const constants_1 = require("../constants");
const types_1 = require("../types");
const DEPOSIT_WALLET_TYPES = {
    Call: [
        { name: "target", type: "address" },
        { name: "value", type: "uint256" },
        { name: "data", type: "bytes" },
    ],
    Batch: [
        { name: "wallet", type: "address" },
        { name: "nonce", type: "uint256" },
        { name: "deadline", type: "uint256" },
        { name: "calls", type: "Call[]" },
    ],
};
async function signBatch(signer, chainId, walletAddress, nonce, deadline, calls) {
    const domain = {
        name: constants_1.DEPOSIT_WALLET_DOMAIN_NAME,
        version: constants_1.DEPOSIT_WALLET_DOMAIN_VERSION,
        chainId,
        verifyingContract: walletAddress,
    };
    const message = {
        wallet: walletAddress,
        nonce: BigInt(nonce),
        deadline: BigInt(deadline),
        calls: calls.map(c => ({
            target: c.target,
            value: BigInt(c.value),
            data: c.data,
        })),
    };
    return signer.signTypedData(domain, DEPOSIT_WALLET_TYPES, message, "Batch");
}
async function buildDepositWalletBatchRequest(signer, args, config) {
    const signature = await signBatch(signer, args.chainId, args.walletAddress, args.nonce, args.deadline, args.calls);
    return {
        type: types_1.TransactionType.WALLET,
        from: args.from,
        to: config.DepositWalletFactory,
        nonce: args.nonce,
        signature,
        depositWalletParams: {
            depositWallet: args.walletAddress,
            deadline: args.deadline,
            calls: args.calls,
        },
    };
}
function buildDepositWalletCreateRequest(from, config) {
    return {
        type: types_1.TransactionType.WALLET_CREATE,
        from,
        to: config.DepositWalletFactory,
    };
}
